name: script-run
on: [push]
jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run build script
        run: './.github/scripts/build.sh'
        shell: bash